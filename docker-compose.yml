services:
  strata:
    build: .
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
      PYTHONUNBUFFERED: "1"
      STRATA_PROJECTS_DIR: /var/data/projects
    volumes:
      - strata-projects:/var/data/projects

volumes:
  strata-projects:
